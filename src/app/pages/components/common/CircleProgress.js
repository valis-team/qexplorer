import { CircularProgress } from '@mui/material';

export default function CircleProgress() {
  return (
    <div className="h-256 flex justify-center items-center">
      <CircularProgress />
    </div>
  );
}
